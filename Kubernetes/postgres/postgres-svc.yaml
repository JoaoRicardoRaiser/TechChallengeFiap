apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  type: NodePort
  selector:
    app: postgres
  ports:
    - port: 5432        # porta interna do container
      targetPort: 5432  # porta do Pod/container
      nodePort: 30007   # porta exposta na máquina (entre 30000-32767)