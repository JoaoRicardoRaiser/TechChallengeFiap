apiVersion: apps/v1
kind: Deployment
metadata:
  name: techchallenge-get
spec:
  replicas: 2
  selector:
    matchLabels:
      app: techchallenge-get
  template:
    metadata:
      labels:
        app: techchallenge-get
    spec:
      containers:
        - name: techchallenge-get
          image: joaoraiser12/techchallenge-get:2.0
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          envFrom:
            - secretRef:
                name: techchallenge-get-secrets
